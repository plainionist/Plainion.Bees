<Window x:Class="Plainion.GatedCheckIn.Shell"
        xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
        xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
        Title="Plainion - Gated CheckIn" 
        Width="800" Height="500">
    <Grid Margin="5">
        <Grid.RowDefinitions>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="Auto"/>
            <RowDefinition Height="*"/>
        </Grid.RowDefinitions>

        <Grid Grid.Row="0">
            <Grid.ColumnDefinitions>
                <ColumnDefinition Width="Auto"/>
                <ColumnDefinition Width="*"/>
            </Grid.ColumnDefinitions>

            <TextBlock Grid.Column="0" Text="Solution" VerticalAlignment="Center"/>
            <TextBox Grid.Column="1" Text="{Binding Solution}" Margin="5,0,0,0"/>
        </Grid>

        <CheckBox Grid.Row="1" Content="Build" IsEnabled="False" IsChecked="True" Margin="0,5,0,0"/>

        <CheckBox Grid.Row="2" Content="Tests" IsChecked="{Binding RunTests}" Margin="0,5,0,0"/>

        <CheckBox Grid.Row="3" Content="Check In" IsChecked="{Binding CheckIn}" Margin="0,5,0,0"/>
        
        <Button Grid.Row="4" Content="GO !" HorizontalAlignment="Center" Margin="0,10,0,10" Command="{Binding GoCommand}"/>
        
        <ListBox Grid.Row="5" ItemsSource="{Binding Messages}">
            <ListBox.Style>
                <Style TargetType="{x:Type ListBox}">
                    <Style.Triggers>
                        <DataTrigger Binding="{Binding Succeeded}" Value="True">
                            <Setter Property="BorderBrush" Value="Green"/>
                            <Setter Property="BorderThickness" Value="3"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Succeeded}" Value="False">
                            <Setter Property="BorderBrush" Value="Red"/>
                            <Setter Property="BorderThickness" Value="3"/>
                        </DataTrigger>
                        <DataTrigger Binding="{Binding Succeeded}" Value="null">
                            <Setter Property="BorderBrush" Value="Black"/>
                            <Setter Property="BorderThickness" Value="1"/>
                        </DataTrigger>
                    </Style.Triggers>
                </Style>
            </ListBox.Style>
        </ListBox>
    </Grid>
</Window>
